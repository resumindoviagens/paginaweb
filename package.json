{
  "name": "resumindo-viagens-website-chatbox",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check script.js && node --check api/chat.js && node --check api/end-session.js"
  }
}
